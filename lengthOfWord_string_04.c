//single word length
#include<stdio.h>
#include<string.h>
int main(){
     char name[]= "mr pavel , how are u today! \
Hello mother father . After all life is a trap";
     int len;
     len = strlen(name);
     printf("%d",len);
}