//area of rectangle 
#include<stdio.h>
int main(){
    float length , width,area;
    scanf("%f%f",&length,&width);
    {
        area = length*width;
    }
    printf("area = %.2f",area);
    return 0;
}